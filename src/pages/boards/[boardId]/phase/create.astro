---
import AppLayout from "@layouts/AppLayout.astro";
import FullScreenScaffold from "@components/app/FullScreenScaffold.astro";
import Overlay from "@components/common/Overlay";
import CreatePhase from "@components/boards/CreatePhase";

export function getStaticPaths() {
  return [
    {params: {boardId: 1}},
  ];
}

const { boardId } = Astro.params;
---

<AppLayout title="Crear etapa">
    <Overlay client:load />
    <FullScreenScaffold title="Crear etapa" backLink="/boards/1">
        <CreatePhase client:load />
    </FullScreenScaffold>
</AppLayout>